{
  "schema": {
    "name": "prompt-template",
    "version": "1.0"
  },
  "template": {
    "id": "empty_standard_texture",
    "revision": 1,
    "display_name": "Empty Standard Texture",
    "description": "Passthrough prompt for testing standard texture generation. The subject is sent as the full prompt with no extra wording.",
    "asset_type": "texture",
    "pattern": "{subject}",
    "placeholders": ["subject"],
    "required_placeholders": ["subject"]
  }
}
